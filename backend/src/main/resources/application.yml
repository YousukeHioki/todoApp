spring:
  application:
    name: todoapp
  mvc:
    cors:
      allowed-origins: http://localhost:5173
      allowed-methods: GET,POST,PUT,DELETE,OPTIONS

aws:
    dynamodb-url: ${AWS_DYNAMODB_URL:default}
    table-name: ${AWS_DYNAMODB_URL:LocalTodoTable}
